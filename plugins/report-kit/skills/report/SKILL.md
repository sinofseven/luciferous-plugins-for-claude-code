---
name: report
description: report タスクを実行します。ドキュメントを調査し、結果をログに記録します。Use when the user wants to execute a report task, investigate documents, or says /report. Also trigger when the user says "report タスクを進める", "0001 を実行" or similar with a task number.
argument-hint: [task-number-or-filename]
---

report タスクを実行します。ドキュメントを調査し、結果をログに記録します。

## 引数

$ARGUMENTS

引数としてタスク番号またはファイル名を指定できます（例: `0001` または `0001_research-topic`）。**引数で番号が指定された場合は、フォールバックロジックを使わず、必ずその番号のタスクを対象とすること**。対象タスクファイルに `## 完了サマリー` が含まれている場合は「タスク {xxxx} はすでに実行済みです（完了サマリーが存在します）」と報告してスキルを終了する。`## 完了サマリー` がない場合のみ、通常の手順に進む。

引数がない場合のみ、`reports/` 内の未完了タスク（`## 完了サマリー` を含まないファイル）のうち番号が最大のものを自動選択します。

## 手順

1. `reports/` から対象タスクファイルを読み込み、タスク内容を理解する
   - タスクファイルのパス: `reports/{xxxx}_{title}/{xxxx}_{title}.md`
2. タスクファイルに「目的」（Why: なぜこの調査が必要か）に該当するセクションがあるか確認する
   - `## 要望` や `## 知りたいこと` は What/How に該当するため、目的（Why）とはみなさない
   - セクション名は問わない（`## 目的`、`## 背景` など）。内容を読み、作業の動機・背景・理由が記載されているか判断する
   - 目的に該当するセクションが**ない場合**: ユーザーに「目的（Why）に該当するセクションが見つかりません。目的セクションを追加することを推奨しますが、なくても続行できます。続行しますか？」と確認する
     - ユーザーが続行を選択した場合: そのまま調査に進む
     - ユーザーが追加を選択した場合: report ファイルに目的セクションを追加するよう案内し、追加後に再実行を求める
3. 関連するドキュメントを網羅的に調査する
4. ログファイルを `reports/{xxxx}_{title}/log.md` に作成し、**調査した内容を全て記録**する（詳細は後述）
5. report ファイルに `## 調査サマリー` と `## 完了サマリー` を追記する

## ログファイルの記録方針

> **ログ記録の原則**: ログはインタラクティブセッションで得た情報・判断・経緯の**完全な記録**である。report ファイルの `## 調査サマリー` は要約版、ログは完全版。情報の要約・省略・圧縮をしてはならない。

ログファイル（`reports/{xxxx}_{title}/log.md`）には以下を記録する:

- **調査ファイル一覧**: 調査したドキュメントのパス一覧（調査の網羅性確認用）
- **調査結果**: 調べたファイルごとに、発見した具体的な事実・構造・記述内容を**省略せず詳細に**記述する
  - 「〜を確認した」のような結論だけでなく、確認した内容そのものを書く
  - インタラクティブセッションで表示された内容と同等の情報量を記録する（要約禁止）
  - 引用・具体例・行番号を含める
- **調査アプローチ**: 調査の進め方、検索キーワード、調査経緯を記録する
- **会話内容**: ユーザーとのやり取りを時系列で省略せず記録する
- **判断・意思決定**: 調査上の判断・解釈のトレードオフを記録する
- **問題・疑問点**: 調査中に発生した問題や未解決の疑問を記録する

詳細なテンプレートは `references/report-workflow.md` を参照すること。

## 注意事項

- **git commit や `/commit` コマンドを自動実行しないこと。** コミットはユーザーが明示的に指示した場合のみ行う。
